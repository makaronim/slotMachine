export enum GAMEENDPOINTS {
    GAME_ROLL = '/game/roll',
    GAME_CASHOUT = '/game/cashout',
    GAME_START = '/game/start',
}

export enum SIGNIN {
    SIGN_IN = '/auth/login',
}

export enum USER {
    CREATE_USER = '/user/create',
}
